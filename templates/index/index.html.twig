{% extends 'base.html.twig' %}

{% block title %}Feed{% endblock %}

{% block body %}

    <div class="container">
        {% include 'parts/items.html.twig' %}
    </div>

    {{ pagination_render(maxPages, page, 'index') }}

{% endblock %}
