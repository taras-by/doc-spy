{% extends 'base.html.twig' %}

{% block title %}Feed{% endblock %}

{% block body %}

    <div class="container">
        {% embed "parts/items_header.html.twig" %}
            {% block title %}Feed{% endblock %}
        {% endembed %}
        {% include 'parts/items.html.twig' %}
    </div>

    {{ pagination_render(maxPages, page, 'feed') }}

    <hr>

    {{ tags_render() }}

{% endblock %}
